<%= form_for(@monkey) do |f| %>
  <% if @monkey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monkey.errors.count, "error") %> prohibited this monkey from being saved:</h2>

      <ul>
      <% @monkey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
